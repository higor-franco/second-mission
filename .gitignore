backend/cmd/server/server
backend/cmd/server/server.exe
backend/server.exe
backend/*.exe
frontend/dist/
frontend/node_modules/
e2e/node_modules/
.venv/
.env
.claude/launch.json

# DD-214s and other service-history documents are processed in memory
# only (see tech-stack and ADR 007). Nothing of this shape ever belongs
# in the repo — belt-and-suspenders against accidental `git add -A`.
*.pdf
DD214_*
dd214_*
